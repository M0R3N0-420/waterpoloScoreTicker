# Waterpolo Score Ticker

Aplicación web para mostrar resultados de partidos de waterpolo en tiempo real con una interfaz moderna y responsive.

## 🚀 Características

### 📊 Visualización de Partidos
- **Tarjetas de partidos** con información completa:
  - Equipos locales y visitantes
  - Marcador en tiempo real
  - Estado del partido (EN VIVO, FINALIZADO, PROGRAMADO)
  - Hora y competencia
  - Período actual del partido

### 📅 Organización por Fechas
- **Separadores de fecha** que muestran:
  - Día de la semana (Lun, Mar, Mié, etc.)
  - Fecha formateada (Ene 15, Feb 20, etc.)
- **Agrupación automática** de partidos por fecha
- **Orden cronológico** de fechas y horas

### 🎨 Interfaz de Usuario
- **Diseño moderno** con TailwindCSS
- **Navegación horizontal** con flechas de scroll
- **Indicadores visuales** para partidos en vivo
- **Layout responsive** y optimizado

### 🔄 Datos en Tiempo Real
- **Integración con Supabase** para gestión de datos
- **Actualizaciones automáticas** de la base de datos
- **Ordenamiento inteligente** por fecha y hora

## 🛠️ Stack Tecnológico

- **Frontend**: React 19 + Vite
- **Estilos**: TailwindCSS 4
- **Base de Datos**: Supabase
- **Lenguaje**: JavaScript/JSX

## 📦 Instalación

### Prerrequisitos
- Node.js 18+
- Cuenta en Supabase

### Pasos de Instalación

1. **Clonar el repositorio**
   ```bash
   git clone <repository-url>
   cd waterpoloScoreTicker
   ```

2. **Instalar dependencias**
   ```bash
   npm install
   ```

3. **Configurar Supabase**
   - Crear un proyecto en [Supabase](https://supabase.com)
   - Ejecutar el script `database-setup.sql` en el SQL Editor
   - Copiar las credenciales desde Project Settings > API

4. **Configurar variables de entorno**
   ```bash
   cp .env.example .env
   ```
   Editar `.env` con tus credenciales:
   ```
   VITE_SUPABASE_URL=https://your-project-id.supabase.co
   VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
   ```

5. **Iniciar la aplicación**
   ```bash
   npm run dev
   ```

## 🗄️ Estructura de la Base de Datos

### Tabla `games`
```sql
CREATE TABLE games (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  date TEXT NOT NULL,           -- YYYY-MM-DD
  time TEXT NOT NULL,           -- HH:MM
  competition TEXT NOT NULL,    -- Nombre de la competencia
  home_team TEXT NOT NULL,      -- Equipo local
  away_team TEXT NOT NULL,      -- Equipo visitante
  home_score INTEGER,          -- Marcador local
  away_score INTEGER,          -- Marcador visitante
  period TEXT,                 -- Período actual (1T, 2T, 3T, 4T)
  status TEXT NOT NULL DEFAULT 'PROGRAMADO', -- EN VIVO, FINALIZADO, PROGRAMADO
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

## 📁 Estructura del Proyecto

```
src/
├── components/
│   ├── WaterpoloGameCard.jsx     # Tarjeta individual de partido
│   ├── WaterpoloDateSeparator.jsx # Separador de fechas
│   ├── GamesContainer.jsx        # Contenedor con navegación
│   ├── NavigationArrows.jsx      # Flechas de navegación
│   └── LoadingSpinner.jsx        # Indicador de carga
├── services/
│   └── gamesService.js           # Servicio de Supabase
├── config/
│   └── supabase.js              # Configuración de Supabase
├── App.jsx                       # Componente principal
├── main.jsx                      # Punto de entrada
└── index.css                     # Estilos globales
```

## 🎯 Componentes Principales

### WaterpoloGameCard
Muestra información completa de un partido:
- Estado con indicador animado para partidos en vivo
- Equipos y marcadores
- Competencia y período
- Diseño compacto de 48x24

### WaterpoloDateSeparator
Separador visual para agrupar partidos:
- Día de la semana en negrita
- Fecha formateada
- Diseño vertical de 16x24

### GamesContainer
Contenedor con navegación horizontal:
- Scroll suave con flechas
- Ocultación automática de scrollbar
- Responsive y accesible

## 🔧 Scripts Disponibles

```bash
npm run dev      # Servidor de desarrollo
npm run build    # Build para producción
npm run preview  # Preview del build
npm run lint     # Linting del código
```

## 🌐 Despliegue

### Vercel
1. Conectar repositorio a Vercel
2. Configurar variables de entorno
3. Despliegue automático

### Netlify
1. Conectar repositorio a Netlify
2. Configurar build command: `npm run build`
3. Configurar publish directory: `dist`
4. Añadir variables de entorno

## 🔄 Flujo de Datos

1. **App.jsx** solicita datos via `fetchGames()`
2. **gamesService.js** consulta Supabase
3. **Datos ordenados** por fecha y hora
4. **Agrupación** por fecha en `groupGamesByDate()`
5. **Renderizado** de separadores y tarjetas

## 🎨 Personalización

### Colores y Estilos
- Modificar `tailwindcss` en `index.css`
- Variables CSS en componentes individuales

### Estados de Partido
- `EN VIVO`: Indicador rojo animado
- `FINALIZADO`: Texto estático
- `PROGRAMADO`: Muestra hora del partido

## 🐛 Solución de Problemas

### Problemas Comunes

**Partidos no se muestran**
- Verificar conexión a Supabase
- Revisar variables de entorno
- Comprobar datos en la tabla `games`

**Fechas mal formateadas**
- Verificar formato `YYYY-MM-DD`
- Revisar zona horaria en Supabase

**Estilos no aplican**
- Reiniciar servidor de desarrollo
- Limpiar caché del navegador

## 📝 Licencia

MIT License - Ver archivo LICENSE para detalles

## 🤝 Contribuciones

1. Fork del repositorio
2. Crear feature branch
3. Commit de cambios
4. Push al branch
5. Crear Pull Request

## 📞 Soporte

Para reportar issues o solicitar features:
- Crear issue en GitHub
- Contactar al equipo de desarrollo

---

**Desarrollado con React + Supabase + TailwindCSS**
